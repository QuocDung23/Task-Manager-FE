/**
 * Dark theme tokens.
 *
 * Currently inactive. Registered in `theme-tokens.ts` as a comment, so
 * importing here will surface it through the type system when dark mode
 * ships. Uncomment the entry in `themes` to enable.
 *
 * Mirror in `src/index.css` `.dark` block.
 */

// import type { ThemeTokens } from "./types";
//
// export const darkTheme: ThemeTokens = {
//   background: "oklch(0.18 0.012 60)",
//   foreground: "oklch(0.96 0.006 70)",
//   // ...
// };

export {};
